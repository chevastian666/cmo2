/**
 * Type definitions for Zustand middleware
 * By Cheva
 */

// Re-export types from zustand
export type { StateCreator, StoreMutatorIdentifier } from 'zustand';
export type { StateStorage } from 'zustand/middleware';